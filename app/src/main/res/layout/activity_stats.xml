<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical"
                  android:padding="@dimen/padding_normal">

        <TextView
            style="@style/header_chart_title"
            android:textAppearance="@style/text_chart_title"
            android:text="@string/ideas_per_day"/>

        <hr.matvidako.ideamachine.stats.chart.IdeasPerDayChart
            android:id="@+id/ideas_per_day"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginBottom="@dimen/padding_large"/>

        <TextView
            style="@style/header_chart_title"
            android:textAppearance="@style/text_chart_title"
            android:text="@string/total_ideas"/>

        <hr.matvidako.ideamachine.stats.chart.TotalIdeasChart
            android:id="@+id/total_ideas"
            android:layout_width="match_parent"
            android:layout_height="200dp"/>
    </LinearLayout>
</android.support.v4.widget.NestedScrollView>